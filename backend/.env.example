# Optional: set MONGO_URI to use your local/remote MongoDB.
# If not set, the server will use an in-memory MongoDB instance (no installation needed).
# Example:
# MONGO_URI=mongodb://localhost:27017/memoryapp
JWT_SECRET=dev_secret_key
PORT=5000