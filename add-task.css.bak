body, html { margin:0; padding:0; }
/* Add Task Form Styling */

.task-form-card {
    display: flex;
    flex-direction: column;
    gap: 16px;
    padding: 20px;
    margin-bottom: 8px;
}

.form-group {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.form-label {
    font-weight: 600;
    font-size: 14px;
    color: var(--text-dark);
}

/* Task Dropdown Styling */
.task-dropdown {
    padding: 14px 16px;
    border: 2px solid #d0d0d0;
    border-radius: 12px;
    font-size: 14px;
    font-family: inherit;
    background: white;
    cursor: pointer;
    transition: all 0.2s ease;
    color: var(--text-dark);
}

.task-dropdown:hover,
.task-dropdown:focus {
    border-color: var(--primary-blue);
    outline: none;
    box-shadow: 0 0 0 3px rgba(184, 224, 240, 0.2);
}

/* Form Input Styling */
.form-input {
    padding: 14px 16px;
    border: 2px solid #d0d0d0;
    border-radius: 12px;
    font-size: 14px;
    font-family: inherit;
    transition: all 0.2s ease;
    background: white;
}

.form-input::placeholder {
    color: #aaa;
}

.form-input:hover,
.form-input:focus {
    border-color: var(--primary-blue);
    outline: none;
    box-shadow: 0 0 0 3px rgba(184, 224, 240, 0.2);
}

/* Textarea Styling */
.form-textarea {
    padding: 14px 16px;
    border: 2px solid #d0d0d0;
    border-radius: 12px;
    font-size: 14px;
    font-family: inherit;
    resize: vertical;
    min-height: 100px;
    transition: all 0.2s ease;
    background: white;
}

.form-textarea::placeholder {
    color: #aaa;
}

.form-textarea:hover,
.form-textarea:focus {
    border-color: var(--primary-blue);
    outline: none;
    box-shadow: 0 0 0 3px rgba(184, 224, 240, 0.2);
}

/* Priority Section Styling */
.priority-section {
    display: flex;
    flex-direction: column;
    gap: 12px;
    padding: 0 16px;
    margin: 8px 0;
}

.priority-label {
    font-weight: 700;
    font-size: 14px;
    color: var(--text-dark);
}

.priority-buttons {
    display: flex;
    gap: 10px;
    justify-content: space-evenly;
}

/* Priority Button Styling */
.priority-btn {
    flex: 1;
    padding: 12px 16px;
    border: 2px solid #d0d0d0;
    border-radius: 12px;
    font-weight: 600;
    font-size: 14px;
    background: white;
    color: var(--text-dark);
    cursor: pointer;
    transition: all 0.2s ease;
}

.priority-btn:hover {
    border-color: var(--primary-blue);
    background: rgba(184, 224, 240, 0.1);
}

.priority-btn.active {
    background: var(--primary-blue);
    color: white;
    border-color: var(--primary-blue);
}

/* Action Buttons */
.action-buttons {
    display: flex;
    gap: 12px;
    padding: 16px;
    margin-top: 8px;
}

.btn-cancel,
.btn-generate {
    flex: 1;
    padding: 14px 20px;
    border: none;
    border-radius: 12px;
    font-weight: 700;
    font-size: 15px;
    cursor: pointer;
    transition: all 0.3s ease;
}

.btn-cancel {
    background: #c8d4dc;
    color: var(--text-dark);
}

.btn-cancel:hover {
    background: #b8c8d4;
    transform: translateY(-2px);
}

.btn-generate {
    background: var(--primary-blue);
    color: white;
    box-shadow: 0 4px 12px rgba(184, 224, 240, 0.3);
}

.btn-generate:hover {
    background: #a0d4e8;
    box-shadow: 0 6px 16px rgba(184, 224, 240, 0.4);
    transform: translateY(-2px);
}

@media (max-width: 480px) {
    .priority-buttons {
        flex-direction: column;
    }

    .priority-btn {
        width: 100%;
    }

    .action-buttons {
        flex-direction: column;
    }

    .btn-cancel,
    .btn-generate {
        width: 100%;
    }
}
